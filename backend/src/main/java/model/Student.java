package main.java.model;

public class Student extends User {
    public Student(String email, String cpf, String name, String birthday) {
        super(email, cpf, name, birthday, birthday);
    }
}
