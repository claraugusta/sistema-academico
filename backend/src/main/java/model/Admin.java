package main.java.model;

public class Admin extends User {
    public Admin(String email, String cpf, String name, String birthday, String password) {
        super(email, cpf, name, birthday, password);
    }
}
