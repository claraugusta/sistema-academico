package main.java.model;

public class Professor extends User {
    public Professor(String email, String cpf, String name, String birthday, String password) {
        super(email, cpf, name, birthday, password);
    }
}
